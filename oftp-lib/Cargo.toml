[package]
name = "oftp-lib"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "2.0.18"
tokio = { version = "1", optional = true, features = ["io-util"] }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt", "io-util"] }

[features]
default = []
async = ["dep:tokio"]
